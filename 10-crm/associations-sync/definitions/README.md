# Association Definitions

This folder owns association label metadata and type definitions pulled from HubSpot.

## Power

- Track default and custom association labels.
- Cache `associationTypeId` values for sync jobs and migration tooling.
- Detect drift when labels are changed in HubSpot.

