# Association Mappings

This folder owns the normalized map between external relationship semantics and HubSpot association type IDs.

## Power

- Resolve which relationships should exist.
- Store default versus custom label mappings.
- Keep relationship rules separate from sync transport code.

