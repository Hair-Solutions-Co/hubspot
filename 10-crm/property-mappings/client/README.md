# Property Mappings Client

This folder is for the UI that lets operators or agents inspect fields and map them safely.

## Power

- Fetch external field definitions from the local backend.
- Present HubSpot property definitions for mapping.
- Create, edit, and review mapping choices before sync jobs use them.

## Local Commands

- `npm start`
- `npm run build`
- `npm test`

