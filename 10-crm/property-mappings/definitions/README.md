# Property Definitions

This folder owns normalized property metadata used by mapping tools and sync services.

## Power

- Cache HubSpot property names, types, options, and constraints.
- Store external system field definitions next to HubSpot metadata.
- Support diffing, drift detection, and mapping validation.

