# Breeze

This folder owns HubSpot AI and app-feature surfaces that do not belong to a narrower API family yet.

**Scope note:** Customer Agent / Breeze features are **not** enumerated as separate strings in `hubspot-internal-app/src/app/app-hsmeta.json`. Use HubSpot product docs for Breeze; this folder is for specs and references alongside the rest of the workspace.

## Notes

- Keep Breeze-specific project or app-feature work here.
- Do not invent fake public API boundaries when HubSpot only exposes the capability through projects or product UI.

