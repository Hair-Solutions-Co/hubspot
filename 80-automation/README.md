# Automation

This folder owns workflow-facing integration work.

**Scope source of truth:** `hubspot-internal-app/src/app/app-hsmeta.json` (HSC Internal Integrations).

## Scopes in the internal app manifest

- `automation`

## Notes

- HubSpot exposes workflow-adjacent app features and custom actions, but not every workflow UI operation is represented as a public CRUD API.
- Keep automation specs, custom actions, and workflow-trigger inputs here.

